---
- name: Daily Operations
  hosts: all
  become: true
  gather_facts: true
  pre_tasks:
    - name: Include setup environment task
      ansible.builtin.include_tasks: tasks/setup_environment.yml
  tasks:
    - name: Include operations role
      ansible.builtin.include_role:
        name: operations
